# Canonical agent_proto frame fixtures — SHARED SOURCE OF TRUTH for the C CLI and the
# UE client (packetsonde-ue/Fixtures/agent_proto mirrors these). Both repos assert their
# request builders / parsers against these so the wire model cannot drift. A protocol
# change updates BOTH copies in lockstep. Verbs: audit, probe (now), recipe (next spec).
